export org.example.mysteryboxlearn.box.category.entity.MysteryBoxCategory

input MysteryBoxCategoryInput {
    #allScalars(MysteryBoxCategory)

    id
}

specification MysteryBoxCategorySpec {
    #allScalars

    like /i(name)

    like /i(description)

    id(creator)
}