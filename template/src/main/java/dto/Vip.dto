export org.example.mysteryboxlearn.vip.root.entity.Vip

input VipInput {
    #allScalars(Vip)

    id

    id(user)
}

specification VipSpec {
    #allScalars

    ge(endTime)

    le(endTime)

    id(user)

    id(creator)
}