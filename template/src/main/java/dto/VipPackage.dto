export org.example.mysteryboxlearn.vip.pack.entity.VipPackage

input VipPackageInput {
    #allScalars(VipPackage)

    id
}

specification VipPackageSpec {
    #allScalars

    like /i(name)

    id(creator)
}